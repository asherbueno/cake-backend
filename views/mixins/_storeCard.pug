mixin storeCard(store = {})
  .store
    .store__hero
      .store__actions
      img(src=`/uploads/${store.photo}`)
      br
      br
      br
      br
      br
      br
    .store__details
      h2.title #{store.name}
      p= store.description.split(' ').slice(0, 25).join(' ')
